streamlit>=1.46.0
transformers>=4.40.0
datasets>=2.10.0
huggingface_hub>=0.20.0
trl>=0.8.0
torch>=2.0.0       # For MPS support, PyTorch 1.12+ is generally needed. Standard pip install should work.
accelerate>=0.25.0
peft>=0.10.0       # For LoRA. QLoRA via bitsandbytes is not supported on MPS.
sentencepiece      # Often required by tokenizers

# Note for MPS (Apple Silicon) users:
# - PyTorch will use the 'mps' backend for GPU acceleration.
# - bitsandbytes for 4-bit QLoRA is NOT compatible with MPS.
#   This application will disable QLoRA options when targeting MPS.
#   Standard LoRA fine-tuning via PEFT is supported.
